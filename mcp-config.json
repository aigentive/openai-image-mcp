{
  "description": "OpenAI GPT-Image-1 MCP Server configuration for Claude Desktop",
  "mcpServers": {
    "openai-image-mcp": {
      "command": "sh",
      "args": [
        "-c",
        "poetry run python -m openai_image_mcp.server 2> mcp_server_stderr.log"
      ],
      "cwd": "/Users/lazabogdan/Code/openai-image-mcp",
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "LOG_LEVEL": "${LOG_LEVEL:-INFO}"
      }
    }
  }
}